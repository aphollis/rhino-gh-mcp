{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "rhino-gh-mcp/contracts/space.tessellate.response",
  "title": "space.tessellate response",
  "description": "Wire contract for the space.tessellate listener command (spatial/PROTOCOL.md section 1). vertices_b64 = base64 little-endian float32 xyz triples; indices_b64 = base64 little-endian int32 triangle triples. Any platform adapter must conform; validate with tools/validate-protocol.mjs.",
  "type": "object",
  "required": ["vertices_b64", "indices_b64", "vertexCount", "triangleCount", "toleranceEstimate", "units", "sceneVersion"],
  "properties": {
    "vertices_b64": { "type": "string", "contentEncoding": "base64" },
    "indices_b64": { "type": "string", "contentEncoding": "base64" },
    "vertexCount": { "type": "integer", "minimum": 1 },
    "triangleCount": { "type": "integer", "minimum": 1 },
    "toleranceEstimate": { "type": "number", "minimum": 0 },
    "units": { "type": "string", "minLength": 1 },
    "sceneVersion": { "type": "integer", "minimum": 1 }
  }
}
